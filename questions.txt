Какой оператор используется для начала цикла for в C++? 
1) while 
2) do 
3) for 
4) loop  
3
Какая функция используется для выполнения задержки в C++? 
1) wait() 
2) delay() 
3) sleep() 
4) pause()  
2
Какой оператор используется для перехода к следующей итерации цикла в C++? 
1) continue 
2) break 
3) return 
4) goto  
1
Какой оператор используется для выхода из цикла в C++? 
1) while 
2) break 
3) continue 
4) return  
2
Какая функция используется для генерации случайных чисел в C++? 
1) random() 
2) rand() 
3) randomize() 
4) srand()  
4
Какой цикл в C++ выполняет тело цикла хотя бы один раз, даже если условие ложно? 
1) while 
2) for 
3) do-while 
4) foreach  
3
Какой оператор используется для определения блока кода в C++? 
1) {} 
2) () 
3) [] 
4) <>  
1
Какой оператор используется для увеличения значения переменной на 1 в C++? 
1) += 
2) ++ 
3) = 
4) /=  
2
Какой цикл в C++ может быть использован для перебора элементов массива? 
1) while 
2) for 
3) do-while 
4) foreach  
4
Какой оператор используется для уменьшения значения переменной на 1 в C++? 
1) -- 
2) ++ 
3) -= 
4) *=  
1
Какой оператор используется для определения условия в цикле while в C++? 
1) == 
2) > 
3) < 
4) !=  
4
Какой оператор используется для определения условия в цикле for в C++? 
1) == 
2) > 
3) < 
4) !=  
1
Какой цикл в C++ выполняется бесконечное количество раз? 
1) while(true) 
2) for(;;) 
3) do-while(true) 
4) все вышеперечисленные  
4
Какой оператор используется для определения шага в цикле for в C++? 
1) ++ 
2) -- 
3) += 
4) *=  
1
Какой оператор используется для определения конца цикла for в C++? 
1) ; 
2) 
3) , 
4) .  
1
Какой цикл лучше использовать, когда нужно выполнить блок кода хотя бы один раз?
1) for 
2) while
3) do-while
4) foreach
3
Какой оператор используется для прерывания текущей итерации цикла и перехода к следующей? 
1) break 
2) continue 
3) goto 
4) return  
2
Что произойдет, если в условии цикла while написать условие, которое всегда истинно? 
1) Цикл будет выполняться бесконечно 
2) Цикл будет выполняться один раз 
3) Цикл не будет выполнен ни разу 
4) Цикл выполнится, но только если условие внутри цикла будет истинным  
1
Какие из перечисленных циклов являются предопределенными циклами? 
1) for 
2) while 
3) do-while 
4) все перечисленные  
4
Какая из функций используется для генерации случайных чисел в языке C++? 
1) srand() 
2) rand() 
3) random() 
4) randomize()  
2
Что произойдет, если в условии цикла for написать две команды, разделенные запятой? 
1) Команды будут выполнены по очереди 
2) Команды будут выполнены параллельно 
3) Компилятор выдаст ошибку 
4) Это невозможно 
1
В каком случае цикл for будет бесконечным? 
1) Если условие внутри цикла всегда ложно 
2) Если условие внутри цикла всегда истинно 
3) Если не будет задано условие выхода из цикла 
4) Если в теле цикла будет команда break
2
Какое ключевое слово используется для выхода из цикла? 
1) break 
2) continue 
3) return 
4) exit 
1
Какой цикл используется для перебора элементов массива? 
1) for 
2) while 
3) do-while 
4) foreach
4
Какой оператор используется для перехода на определенную метку в программе? 
1) break 
2) continue 
3) goto 
4) return
3
Какой из циклов имеет самый широкий диапазон входных значений? 
1) while 
2) for 
3) do-while 
4) Все циклы имеют одинаковый диапазон
4
Какой тип цикла следует использовать, когда количество итераций заранее неизвестно? 
1) while 
2) for
3) do-while 
4) switch
1
Какой оператор используется для продолжения цикла со следующей итерации? 
1) break 
2) exit 
3) return 
4) continue
4
Что произойдет, если условие в цикле while всегда равно true? 
1) Цикл будет выполняться бесконечно 
2) Цикл выполнится один раз и завершится 
3) Программа завершится с ошибкой 
4) Ни один из вариантов выше
1
Какой оператор используется для перехода на метку в теле цикла? 
1) break 
2) exit 
3) goto 
4) continue
3
Что произойдет, если в цикле do-while условие всегда равно false? 
1) Цикл будет выполняться бесконечно 
2) Цикл выполнится один раз и завершится 
3) Программа завершится с ошибкой 
4) Ни один из вариантов выше
2
